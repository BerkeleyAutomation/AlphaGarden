from setuptools import setup

setup(name='simalphagarden',
      version='0.0.1',
      install_requires=[
          'gym'
      ])

setup(name='wrapperenv',
      version='0.0.1',
      install_requires=[
      ])